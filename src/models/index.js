const { Player } = require('./player')
const { Team } = require('./team')
const { Season } = require('./season')
const Game = require('./game')
const { GameBox } = require('./gameBox')


module.exports = { Player, Team, Season, Game, GameBox }
